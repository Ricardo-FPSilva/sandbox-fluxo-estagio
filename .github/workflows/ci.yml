name: CI
on:
  pull_request:
  push:
    branches:
      - main
jobs:
  validar-html:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validar HTML
        run: npx --yes htmlhint .
